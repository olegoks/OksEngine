#include <Debug/ECS/ECSEditor\auto_OksEngine.DrawLinks.hpp>

#include <imgui.h>
#include <imgui-node-editor/imgui_node_editor.h>


namespace OksEngine {
    void DrawLinks::Update(
        ECS2::Entity::Id entityId, const CallGraphNode* callGraphNode,
        const RunAfter* runAfter, const RunBefore* runBefore) {

        //for (Common::UInt64 i = 0; i < runAfter->entities_.size(); i++) {
           //ax::NodeEditor::Link((int)entityId, , linkInfo.OutputId);
        //}
        //for (auto& linkInfo : m_Links)
           

    };
}